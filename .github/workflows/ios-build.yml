
name: Practice Build

on:
  push:
     branches:
        -main
        

jobs:
  build:
   runs-on: macos-latest
   
    steps:
    
     - uses: actions/checkout@v4
     - name: Print Message
     
     run: echo "Building App"
